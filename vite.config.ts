import { defineConfig } from "vite";

// Configuração Vite padrão, sem plugins ou caminhos internos do Codex.
// A aplicação é compilada pela Vercel através do Next.js (`npm run build`).
export default defineConfig({});
